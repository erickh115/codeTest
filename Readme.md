```
1  Time complexity of a loop is considered as O(n) ,and the ans have 4for loop
O(n4n) since the big-o notation is not about comparing absoulte complexity, so in fact the same as O(n)
```

```
2
```

```
3  change var to let ,let scoped differently than var based variable
   In loops, you can get a fresh binding for each iteration if let declare
   In loops, you only can get a single binding if var declare

```